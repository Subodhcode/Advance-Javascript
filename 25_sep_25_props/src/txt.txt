1.react icon kaise use kar shakte hai (rect icon library)

2.props-> “Props (short for Properties) are used in React to pass data from a parent component to a child component. They make components dynamic and reusable. Props are read-only, meaning a child component cannot modify them. React follows one-way data binding, so data flows from parent to child using props.”

3.slick slider